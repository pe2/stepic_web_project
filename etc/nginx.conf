server {
	listen       80;
        server_name  localhost;

	location  ~* ^.+\.\w$ {	
	    root	 /home/box/web/public;
	}
	
#	location ^~ /uploads/ {
#            root	 /usr/box/web/uploads;
#      	}
}
